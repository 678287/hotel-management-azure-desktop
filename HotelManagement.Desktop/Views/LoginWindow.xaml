<Window x:Class="HotelManagement.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Email:" Margin="0,0,0,5"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="1" Height="25" />

        <TextBlock Text="Password:" Margin="0,15,0,5" Grid.Row="2"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Height="25" />

        <Button Content="Login" Grid.Row="4" Margin="0,20,0,0" Width="80" HorizontalAlignment="Right"
                Click="LoginButton_Click"/>
    </Grid>
</Window>
